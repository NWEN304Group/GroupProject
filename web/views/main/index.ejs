<% layout('layout') -%>

<!-- Main jumbotron for a primary marketing message or call to action -->
<div class="jumbotron" id="navbar-gap">
    <div class="container">
        <h1> NWEN304 SHOP</h1>
        <p>Guess you will like</p>
        <p><a class="btn btn-primary btn-lg" href="/signup" role="button">Signup &raquo;</a></p>
    </div>
</div>

<div class="container">

    <div class="row">

        <% for (var i = 0; i < products.length; i++) { %>
        <div class="col-md-4">
            <a href="/product/<%= products[i]._id %>">
                <div class="thumbnail">
                    <img class='no-resize' src="<%= products[i].image %>">
                    <div class="caption">
                        <h3><%= products[i].name %></h3>
                        <p><%= products[i].category.name %></p>
                        <p>$<%= products[i].price %></p>
                    </div>
                </div>
            </a>
        </div>
        <% } %>
    </div>
    <% /*footer*/ %>
    <ul class="pagination text-center">
        <li>
            <% for(var i = 1; i <= numOfPage ; i++) { %>
            <% if (i === 1)  { %>
        <li><a href="/"><%= i %></a></li>
        <% } else { %>
        <li><a href="/pagenotlogin/<%= i %>"><%= i %></a></li>
        <% } %>

        <% } %>
        </li>
    </ul>
</div>